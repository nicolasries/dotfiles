language: bash

# Use container-based infrastructure for quicker build start-up
sudo: required

addons:
  apt:
    sources:
    - debian-sid    # Grab shellcheck from the Debian repo (o_O)
    packages:
    - shellcheck

script:
 - ./bin/test_dotfiles.sh

matrix:
  fast_finish: true
